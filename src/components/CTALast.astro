---
import { site } from "../data/site";
---

<section class="cta-last" id="book">
  <div class="cta-inner">
    <p class="cta-eyebrow">{site.ctaEyebrow}</p>
    <h2 set:html={site.ctaH2Html} />
    <p class="cta-lede">{site.ctaLede}</p>

    <a href={site.calcom} class="btn-primary btn-lg cta-btn">
      {site.ctaPrimary} <span aria-hidden="true">→</span>
    </a>

    <a href={site.linkedin} class="cta-alt" target="_blank" rel="noopener">
      {site.ctaAlt}
    </a>

    <div class="cta-sig">{site.ctaSig}</div>
  </div>
</section>

<style>
  .cta-last {
    background: var(--bg-ink);
    color: var(--text-on-ink);
    padding: 96px 0 80px;
    border-top: 2px solid var(--accent);
  }
  .cta-inner {
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
    padding: 0 var(--gutter);
  }
  .cta-eyebrow {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--accent);
    margin: 0 0 24px;
  }
  .cta-last h2 {
    font-family: var(--font-display);
    font-size: clamp(30px, 4vw, 48px);
    font-weight: 800;
    letter-spacing: -0.025em;
    line-height: 1.1;
    color: var(--text-on-ink);
    margin: 0 0 20px;
  }
  .cta-last h2 :global(em) {
    font-style: italic;
    color: var(--accent);
    font-weight: 700;
  }
  .cta-lede {
    font-size: 17px;
    line-height: 1.6;
    color: var(--text-on-ink-muted);
    margin: 0 auto 40px;
    max-width: 56ch;
  }
  .cta-btn { margin-bottom: 20px; }
  .cta-alt {
    display: block;
    font-family: var(--font-mono);
    font-size: 13px;
    color: rgba(250,250,250,.52);
    text-decoration: underline;
    text-underline-offset: 4px;
    margin-bottom: 40px;
    transition: color var(--dur-fast) var(--ease);
  }
  .cta-alt:hover { color: var(--accent); }
  .cta-sig {
    font-family: var(--font-mono);
    font-size: 11px;
    color: rgba(250,250,250,.36);
    letter-spacing: 0.04em;
    padding-top: 24px;
    border-top: 1px solid rgba(250,250,250,.12);
  }
  @media (max-width: 720px) {
    .cta-last { padding: 72px 0 64px; }
    .cta-inner { padding: 0 var(--gutter-sm); }
  }
</style>
